d#include <stdio.h>

int main()
{
    printf("C언어의 장벽에 대흉근이 웅장해집니다.");

    int weight;
    scanf("%d", &weight);
    if(weight)
{
       printf("기분이 좋아 벤치프레스를 해 근성장이 되었습니다.");
}
   else
{
       printf("현실의 당신처럼 운동을 하지 않아 근손실이 났습니다.");
}

printf ("벤치프레스를 열심히 해야 합니다.");
    

    return 0;
}
